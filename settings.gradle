rootProject.name = 'java-playground'
include 'jsondata'

def root = 'modules',
    includes = [
            lambda: root
    ]
includes.each { module, parent ->
    include(":$module")
    project(":$module").projectDir = file("$parent/$module")
}
